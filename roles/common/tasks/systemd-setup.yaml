  - name: demon reload 
    ansible.builtin.systemd_service:
      daemon_reload: true

  - name: "start and enable {{component}}" 
    ansible.builtin.service: 
      name: "{{component}}" 
      state: started 
      enabled: yes